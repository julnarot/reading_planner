import { AbstractRestService } from './abstract-rest-service';

describe('AbstractRestService', () => {
  it('should create an instance', () => {
    expect(new AbstractRestService()).toBeTruthy();
  });
});
