import { HttpRequestStatus } from './http-request-status';

describe('HttpRequestStatus', () => {
  it('should create an instance', () => {
    expect(new HttpRequestStatus()).toBeTruthy();
  });
});
